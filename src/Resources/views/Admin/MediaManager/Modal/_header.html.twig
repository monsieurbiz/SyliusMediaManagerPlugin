{{ (folder is not empty ? '<i>' ~ folder ~ '/</i>' : '')|raw  }}{{ (path is not empty ? '<i>' ~ path ~ '</i>' : '')|raw  }}{{ folder is not empty or path is not empty ? ' - ' : '' }}
{{ 'monsieurbiz_sylius_media_manager.ui.choose_file' | trans }}
